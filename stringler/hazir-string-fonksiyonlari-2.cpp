#include <iostream>
#include <cstring>

using namespace std;

int main() {
    /* string txt;
    cout << "Enter a string:";
    getline(cin, txt);
    for (int i = 0; i < txt.size(); i++) {
        // islower sadece kucuk harfleri aliyor.
        if (islower(txt[i])) {
            cout << txt[i];
        }
    }
     */

/*     string txt;
    cout << "Enter a string:";
    getline(cin, txt);
    for (int i = 0; i < txt.size(); i++) {
        // islower sadece buyuk harfleri aliyor.
        if (isupper(txt[i])) {
            cout << txt[i];
        }
    }
    */
    // isprint yazdirilabilir karakterleri yazdirir. yani bosluk falan disinda cogu seyi yazdirir.
    // ispunct noktalama isaretlerini yazdirir.
    // isspace bosluklari alir sadece bosluklari sayabiliriz boyle.
    // tolower yazdiklarini kucuk harfe cevirir.
    // toupper ise buyuk harfe cevirir.

    return 0;
} 